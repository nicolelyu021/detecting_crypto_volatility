# Environment Configuration Template
# Copy this file to .env and fill in your actual values
# NOTE: .env is gitignored - never commit real secrets to GitHub!

# Kafka Configuration
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# MLflow Configuration
MLFLOW_TRACKING_URI=http://localhost:5000
MODEL_VARIANT=
MODEL_RUN_NAME=ml_xgboost
MODEL_RUN_ID=

# API Configuration
PORT=8000
CONFIG_PATH=config.yaml

# Coinbase WebSocket (optional - only needed for live data ingestion)
COINBASE_API_KEY=
COINBASE_API_SECRET=
