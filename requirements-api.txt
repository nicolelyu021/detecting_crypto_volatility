# API-specific requirements (without evidently to avoid Pydantic v2 conflict)

# Core dependencies
confluent-kafka==2.3.0
pyyaml==6.0.1
python-dotenv==1.0.0

# Data processing
pandas>=2.2.2
pyarrow>=17.0.0
numpy>=1.24.0

# MLflow (for model metadata, optional)
mlflow>=2.15.0

# Machine Learning
scikit-learn>=1.3.0
xgboost>=2.0.0

# API and web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
prometheus-client>=0.19.0

