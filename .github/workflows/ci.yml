name: CI

on:
  push:
    branches: [ final-release-12-08 ]
  pull_request:
    branches: [ main, week4, week5, final-release-12-08 ]
  workflow_dispatch:  # Allows manual triggering from GitHub UI

jobs:
  lint-and-format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: Install dependencies
        run: |
          pip install black ruff
      
      - name: Run Black
        run: |
          black --check --diff .
        continue-on-error: false
      
      - name: Run Ruff
        run: |
          ruff check .
        continue-on-error: false

  integration-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
      
      - name: Run integration test
        run: |
          python -m pytest tests/ -v
